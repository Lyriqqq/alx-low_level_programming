#include <stdio.h>
/**
*main -Prints some letters of the alphabets
*
* Return: 0
*/

int main(void)
{
	 /* Declarations */
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
		printf("%d", digit);
	}

	putchar('\n');
	return (0);
}
